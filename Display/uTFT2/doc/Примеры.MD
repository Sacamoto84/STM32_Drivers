#include "main.h"

*****************************************
  Поместить спрайт на LCD
*****************************************
tft.copy(&Sprite, -32, 20);
tft.copyTr(&Cursor, TS[1], TS[2], BLACK);

*****************************************
  Вращение (через класс transformation)
*****************************************
transformation trans(&tft);

Sprite._xPivot = -20;     //Точка вращения спрайта
Sprite._yPivot = 15;

tft._xPivot = 135 / 2;    //Точка экрана, куда помещается точка вращения
tft._yPivot = 240 / 2;

trans.pushRotated(&tft, &Sprite, angle, BLACK);
trans.pushRotated(&tft, &Sprite, angle, WHITE);

tft.copy(&Sprite, 20, 20);

*****************************************
Скроллинг (через класс transformation)
*****************************************
transformation transSpr(&Sprite);

Sprite.SetPixel(25, graphVal, BLUE);

transSpr.setScrollRect(0, 0, 32, 32, BLACK);
transSpr.scroll(-1, 0);
